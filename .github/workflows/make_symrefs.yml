name: make_symrefs

on:
  push:
    branches: [ master ]
    paths:
      - '.github/workflows/make_symrefs.yml'
  schedule:
    - cron: '00 15 14 * *'
  workflow_dispatch:
    inputs:
      fos:
        description: "FOS Version to build"
        required: true
        default: "08_2X"
        type: choice
        options:
          - "alles"
          - "Fos07"
          - "Fos08"
          - "07_5X"
          - "08_0X"
          - "08_2X"
          - "08_5X"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
  queue: max

jobs:

  matrizifizieren:
    permissions:
      packages: read
      contents: read
    container:
      image: ghcr.io/freetz-ng/internal/generate
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.FREETZ_DLTOKEN || secrets.GITHUB_TOKEN }}
      options: --user 1001:1001
    runs-on: ubuntu-24.04
    if: github.repository == 'freetz-ng/freetz-ng' || github.event_name == 'workflow_dispatch'

    outputs:
      gitsha: ${{ steps.parse.outputs.gitsha }}
      matrix: ${{ steps.parse.outputs.matrix }}

    steps:

      - name: clone
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config --global --add safe.directory $GITHUB_WORKSPACE
          git clone https://github-actions:$GITHUB_TOKEN@${GITHUB_SERVER_URL##*/}/$GITHUB_REPOSITORY.git $GITHUB_WORKSPACE
          git checkout $GITHUB_REF_NAME

      - name: parse
        id: parse
        run: |
          FOS="${{ github.event.inputs.fos }}"
          [ -z "$FOS" ] && FOS="08_2X"
          [ "$FOS" = "alles" ] && FOS="07_5X $(sed -rn "s/^config FREETZ_AVM_HAS_FIRMWARE_(0[89])/\1/p" config/avm/availability.in)"
          [ "$FOS" == "Fos07" ] && FOS="07_5X"
          [ "$FOS" != "${FOS#Fos}" ] && FOS="$(sed -rn "s/^config FREETZ_AVM_HAS_FIRMWARE_(${FOS#Fos})/\1/p" config/avm/availability.in)"
          echo "gitsha=\"${{ github.sha }}\"" >> $GITHUB_OUTPUT
          for fos in $FOS; do \
            cat config/avm/availability.in | \
              grep -A999 -m1 "^config FREETZ_AVM_HAS_FIRMWARE_$fos$" | grep -B999 -m2 "^config FREETZ_AVM_HAS_" | \
              sed "s/[&| \t\\()]//g" | sed -rn "s/^FREETZ_TYPE_//p" | grep -vE "^(LANG_|xxxx$)" | sort | \
              while read -r box; do echo -n "$matrix{fos:\"$fos\",box:\"$box\"},"; done; \
          done | \
          sed 's/^/matrix=\[/;s/$/\]/' >> $GITHUB_OUTPUT

      - name: vars
        run: |
          echo "##########################################################"
          echo "gitsha=${{ steps.parse.outputs.gitsha }}"
          echo "matrix=${{ steps.parse.outputs.matrix }}" | sed 's/\[{/\[\n{/;s/},{/},\n{/g;s/},\]/},\n\]/'

  build:
    permissions:
      packages: read
      contents: write
    container:
      image: ghcr.io/freetz-ng/internal/firmware
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.FREETZ_DLTOKEN || secrets.GITHUB_TOKEN }}
      options: --user 1001:1001
    needs: matrizifizieren
    strategy:
      max-parallel: 18
      fail-fast: false
      matrix:
        fritz: ${{ fromJson(needs.matrizifizieren.outputs.matrix) }}
    name: "${{ matrix.fritz.fos }} - ${{ matrix.fritz.box }}"
    runs-on: ubuntu-24.04

    steps:

#     - name: update
#       run: apt-get -y update && apt-get -y upgrade && apt-get -y dist-upgrade
#
#     - name: install
#       run: |
#         DEBIAN_FRONTEND=noninteractive apt-get -y install \
#           locales \
#           pv cpio rsync kmod imagemagick inkscape graphicsmagick subversion git bc unar wget sudo gcc g++ binutils autoconf automake \
#           autopoint libtool-bin make bzip2 libncurses5-dev libreadline-dev zlib1g-dev flex bison patch texinfo gettext pkg-config ecj \
#           perl libstring-crc32-perl ruby gawk libusb-dev unzip intltool libacl1-dev libcap-dev libc6-dev-i386 \
#           lib32ncurses5-dev gcc-multilib bsdmainutils lib32stdc++6 libglib2.0-dev ccache cmake lib32z1-dev libsqlite3-dev sqlite3 libzstd-dev \
#           netcat curl uuid-dev libssl-dev libgnutls28-dev u-boot-tools device-tree-compiler
#
#     - name: locale
#       run: locale-gen en_US.utf8 && locale-gen de_DE.UTF-8 && update-locale

#     - name: sleep
#       run: |
#         SLEEP="$((1 + $(od -A n -t d -N 2 /dev/urandom | tr -d ' ') % 9))"
#         echo "Sleeping $SLEEP seconds ..."
#         sleep $SLEEP

      - name: myips
        run: |
          echo -n "IP: " ; wget -q https://ipaddress.ai    -O - || echo none
#         echo -n "V4: " ; wget -q https://ipaddress.ai -4 -O - || echo none
#         echo -n "V6: " ; wget -q https://ipaddress.ai -6 -O - || echo none

      - name: clone
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NAME: ${{ fromJson(needs.matrizifizieren.outputs.gitsha) }}
        run: |
          git config --global --add safe.directory $GITHUB_WORKSPACE
          git clone https://github-actions:$GITHUB_TOKEN@${GITHUB_SERVER_URL##*/}/$GITHUB_REPOSITORY.git $GITHUB_WORKSPACE
          git checkout $REF_NAME

      - name: cache_load
        uses: actions/cache/restore@v6
        if: always()
        with:
          path: |
            dl/*
            !dl/linux-*.tar.xz
            !dl/*-freetz-*-shared-glibc.tar.lzma
          key: make_cache
          restore-keys: make_cache

      - name: config
        run: |
          FOS="${{ matrix.fritz.fos }}"
          BOX="${{ matrix.fritz.box }}"
          truncate -s0 .config
          echo '# FREETZ_TOOLCHAIN_CCACHE is not set'                                                 >> .config
          echo 'FREETZ_VERBOSITY_FWMOD_2=y'                                                           >> .config
          echo 'FREETZ_VERBOSITY_LEVEL_2=y'                                                           >> .config
          echo 'FREETZ_VERBOSITY_LEVEL=2'                                                             >> .config
          echo 'FREETZ_SERIES_ALL=y'                                                                  >> .config
          echo 'FREETZ_USER_LEVEL_DEVELOPER=y'                                                        >> .config
          echo '# FREETZ_DL_IMAGE_AT_LAST is not set'                                                 >> .config
          echo '# FREETZ_ADD_INACTIVE_SYSTEM is not set'                                              >> .config
          echo 'FREETZ_DL_SITE_USER="https://github.com/Freetz-NG/dl-mirror/releases/download/dl"'    >> .config
          echo 'FREETZ_REAL_DEVELOPER_ONLY__DLIMG="${{ secrets.ACTIONS_IMAGE }}"'                     >> .config
          echo 'FREETZ_REAL_DEVELOPER_ONLY__TCURL="${{ secrets.ACTIONS_DLTCS }}"'                     >> .config
          [ -z "${{ secrets.FREETZ_DLTOKEN }}" ] && \
          echo '# FREETZ_DOWNLOAD_TOOLCHAIN is not set'                                               >> .config ||
          echo 'FREETZ_DOWNLOAD_TOOLCHAIN=y'                                                          >> .config
          [ -z "${{ secrets.FREETZ_DLTOKEN }}" ] && \
          echo '# FREETZ_HOSTTOOLS_DOWNLOAD is not set'                                               >> .config ||
          echo 'FREETZ_HOSTTOOLS_DOWNLOAD=y'                                                          >> .config
          grep -q '^FREETZ_TYPE_' .config || echo "FREETZ_TYPE_$BOX=y"                                >> .config
          grep -q '^FREETZ_TYPE_FIRMWARE_' .config || echo "FREETZ_TYPE_FIRMWARE_$FOS=y"              >> .config
          echo "##########################################################" && echo -n "Lines=" && wc -l .config

      - name: generate
        id: generate
        env:
          FREETZ_DLTOKEN: ${{ secrets.FREETZ_DLTOKEN }}
        run: |
          make olddefconfig kernel-symrefs
          [ -e symrefs-*.tar.lzma ] && echo "ABORT=false" >> "$GITHUB_OUTPUT" || echo "ABORT=true" >> "$GITHUB_OUTPUT" 

      - name: git-diff
        run: |
          echo "##########################################################"
          git diff --no-prefix

      - name: symrefs
        if: steps.generate.outputs.ABORT != 'true'
        run: |
          echo "##########################################################"
          tar tvf symrefs-*.tar.lzma

      - name: result
        if: steps.generate.outputs.ABORT != 'true'
        run: |
          echo "##########################################################"
          ls -lL symrefs-*.tar.lzma
          du -sh $(readlink symrefs-*.tar.lzma)
          sha256sum symrefs-*.tar.lzma

#     - name: cache_clear
#       if: github.repository == 'freetz-ng/freetz-ng'
#       env:
#         ACTIONS_TOKEN: ${{ secrets.ACTIONS_TOKEN }}
#         CACHE_KEY: ${{ github.workflow }}
#       run: |
#         ASSETS="$(curl -s  -H "Accept: application/vnd.github+json"  -H "Authorization: Bearer $ACTIONS_TOKEN"  -H "X-GitHub-Api-Version: 2026-03-10"  "https://api.github.com/repos/${{ github.repository }}/actions/caches")"
#         ASSID="$(echo "$ASSETS" | grep -B7 "\"key\": *\"${CACHE_KEY}\"" | sed -rn 's/ *"id": *([^,]*),*/\1/p')"
#         curl -sL -X DELETE -H "Accept: application/vnd.github+json"  -H "Authorization: Bearer $ACTIONS_TOKEN"  -H "X-GitHub-Api-Version: 2026-03-10"  "https://api.github.com/repos/${{ github.repository }}/actions/caches/$ASSID" && echo "Deleted ASSID=${ASSID:-null} ..." || true
#     - name: cache_save
#       uses: actions/cache/save@v4
#       if: always() && github.repository != 'freetz-ng/freetz-ng'
#       with:
#         path: |
#           dl/*
#           !dl/linux-*.tar.xz
#         key: ${{ github.workflow }}--${{ matrix.fritz.box }}

      - name: release
        uses: ncipollo/release-action@v1
        if: steps.generate.outputs.ABORT != 'true' && github.repository == 'freetz-ng/freetz-ng'
        with:
          owner: Freetz-NG
          repo: freetz-ng
          tag: kernel-symrefs
          name: kernel-symrefs
          body: These files are internally used!
          draft: true
          prerelease: true
          allowUpdates: true
          removeArtifacts: false
          artifacts: "symrefs-*.tar.lzma"
          token: ${{ secrets.GITHUB_TOKEN }}
          replacesArtifacts: false
          artifactErrorsFailBuild: true


