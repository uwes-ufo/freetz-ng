--- configure
+++ configure
@@ -37622,6 +37622,8 @@
     libmagic/readcdf.c libmagic/softmagic.c libmagic/der.c \
     libmagic/buffer.c libmagic/is_csv.c"
 
+# strcasestr is avaliable in all versions of uClibc since 0.9.28, no need to check
+if false; then
   { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for strcasestr" >&5
 printf %s "checking for strcasestr... " >&6; }
   if test "$cross_compiling" = yes
@@ -103572,6 +103574,7 @@
 esac ;;
 esac
 fi
+fi
 
 
 
